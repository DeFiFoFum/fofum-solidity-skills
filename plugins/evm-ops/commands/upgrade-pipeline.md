---
name: upgrade-pipeline
description: Run the evm-ops upgrade pipeline skill. Use the upgrade-pipeline skill to orchestrate the full proxy upgrade workflow.
argument-hint: "[--chain <chain>] [--proxy-admin <addr>] [--safe <addr>] ContractName:oldAddr:newAddr ..."
---

Use the `upgrade-pipeline` skill to run the full proxy upgrade workflow with arguments: $ARGUMENTS
